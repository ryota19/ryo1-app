<h1>kogotoユーザー一覧</h1>

<div class="main user-show">
    <div class="container">
        <div class="user">
            <div class="posts-show-item">
                <h2><%= link_to @user.name, "/users/#{@user.id}/edit" %></h2>
                <p><%= @user.email %></p>
            </div>

            <%= @user.posts.each do |post| %>
                <div class="posts-index-item">
                    <div class="post-right">
                        <%= link_to post.content, "/posts/#{post.id}" %>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
</div>